import Spinner from "@/components/Spinner";

const UserPostPage = () => {
  return (
    <main>
      <h1>user x post</h1>
      <Spinner show={true}></Spinner>
    </main>
  );
};

export default UserPostPage;
